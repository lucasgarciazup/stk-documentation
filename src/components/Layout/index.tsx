import React from 'react';

// import { Container } from './styles';

const Layout: React.FC = (props) => {
  return (
    <div>
        {props.children}
    </div>)
  ;
}

export default Layout;